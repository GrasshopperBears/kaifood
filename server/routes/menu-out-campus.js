import express from "express";

const router = express.Router();

router.get("/", () => {});
router.post("/", () => {});
router.patch("/", () => {});
router.delete("/", () => {});

export default router;
